{
  "name": "@rag/scraper-worker",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@rag/db": "*",
    "@rag/shared": "*",
    "bullmq": "^5.34.0",
    "cheerio": "^1.0.0",
    "playwright": "^1.49.1",
    "robots-parser": "^3.0.1"
  },
  "devDependencies": {
    "tsx": "^4.19.2"
  }
}
